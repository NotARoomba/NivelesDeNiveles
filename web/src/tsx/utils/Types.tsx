export interface DisasterCardProps {
  icon: string;
  title: string;
  description: string;
  delay: number;
}

export interface AdvancementCardProps {
  title: string;
  subtitle: string;
  link: string;
  imagePath: string;
}
